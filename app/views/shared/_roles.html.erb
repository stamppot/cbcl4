<% roller = [] %>

<ul>
	<% @roles.each do |r| %>
		<li>
		<%= if current_user.access? (:role_show) && (current_user.login =~ /jens/) 
			link_to t("roles.#{r.title}"), active_rbac_role_path(r)
	 	else 
			r.title
		end
		%>
		</li>
	<% end %>
</ul>